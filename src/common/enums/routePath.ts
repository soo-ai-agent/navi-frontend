export enum RoutePath {
    START = "/",
    QUESTIONS = "/questions",
    WISH = "/wish",
    RESULT = "/result",
    ANSWER_SUMMARY = "/my-conditions",
    PRODUCTS = "/products",
    PRODUCT_DETAIL = "/product/:name",
}
